<h3>Users:</h3>
<%= will_paginate @users %>

    <ul class="users">
      <% @users.each do |user| %>
        <li>
          <%= link_to user.email, adjust_edit_path(email: user.email) %>
        </li>
      <% end %>
    </ul>
<%= will_paginate @users %>
